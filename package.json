{
  "name": "simoulin.io",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel ./src/index.html",
    "prebuild": "npx rimraf build",
    "build": "rm -rf build && parcel build --public-url ./ ./src/index.html --dist-dir build",
    "start": "parcel ./src/index.html",
    "predeploy": "rm -rf dist && parcel build ./src/index.html --public-url ./ && cp CNAME ./dist && cp README.md ./dist && cp -r slides ./dist && cp .gitmodules ./dist",
    "deploy": "gh-pages -d dist --dotfiles"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/AntoineSimoulin/simoulin.io.git"
  },
  "bugs": {
    "url": "https://github.com/AntoineSimoulin/simoulin.io.git/issues"
  },
  "homepage": "https://github.com/AntoineSimoulin/simoulin.io.git#readme",
  "keywords": [],
  "author": "Antoine Simoulin",
  "license": "ISC",
  "dependencies": {
    "@popperjs/core": "^2.11.6",
    "bootstrap": "^5.2.0-beta1",
    "bootstrap-icons": "^1.9.1",
    "gh-pages": "^4.0.0",
    "popper.js": "^1.16.1",
    "prismjs": "^1.29.0"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.6.0"
  }
}
